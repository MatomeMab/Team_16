@model IEnumerable<Nazox.Models.Employee>

@{
    ViewBag.Title = "Employee";
}

<h2>Employee</h2>

<p><a href="@Url.Action("exportEmployee","Reports")">Export Employee</a></p>
<table class="table">
    <tr>
       
        <th>
            @Html.DisplayNameFor(model => model.EmployeeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeSurname)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.DateEmployed)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.EmployeeType.EmployeeTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeStatu.EmployeeStatusName)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeSurname)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.DateEmployed)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeType.EmployeeTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeStatu.EmployeeStatusName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title.TitleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.SessionID)
        </td>
       
    </tr>
}

</table>
@using (Html.BeginForm("Employee", "Reports", FormMethod.Post))
            {
                <div class="btn-group">
                    <input type="submit" formaction="DownloadPDFReport" value="PDF-Download" class="btn btn-primary" /> 

                </div>
            }
